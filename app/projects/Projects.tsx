"use client";
import React, { useEffect, useLayoutEffect, useRef } from "react";

const Projects = () => {
  return (
    <div
      className="w-full h-full flex max-w-6xl items-center justify-center scroll-mt-20 space-x-8"
      id="projects-section"
    >
    </div>
  );
};

export default Projects;
